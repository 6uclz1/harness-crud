# dependencies / build
node_modules
.next
*.tsbuildinfo
next-env.d.ts

# env (secrets never committed; use .env.example as the template)
.env
.env.local
.env*.local

# misc
coverage
.DS_Store

# テスト観点レポートの中間生成物 (scripts/render-test-report.ts / ci.yml 参照)
vitest-report-*.json
test-perspectives-*.md
